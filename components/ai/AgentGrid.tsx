'use client';
import {AGENTS,AgentKey} from '@/lib/types'; import {Bot,LockKeyhole} from 'lucide-react';
export function AgentGrid({onSelect}:{onSelect?:(key:AgentKey)=>void}){return <div className="grid gap-3 sm:grid-cols-2 xl:grid-cols-3">{AGENTS.map(a=><button key={a.key} onClick={()=>onSelect?.(a.key)} className="card p-4 text-left hover:bg-white/[.04]"><div className="flex items-start justify-between"><span className="flex h-9 w-9 items-center justify-center rounded-xl bg-white/10"><Bot size={18}/></span><span className="badge">AI ROLE</span></div><div className="mt-4 font-medium">{a.name}</div><div className="mt-1 text-xs muted">{a.short}</div>{a.key==='personal_assistant'&&<div className="mt-3 text-[11px] flex gap-1 muted"><LockKeyhole size={12}/> Private to Administrator</div>}</button>)}</div>}
